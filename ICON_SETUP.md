# สร้างไอคอนแอพ (launcher icon) จากโลโก้ร้าน

ไฟล์ `resources/icon.png` (โลโก้ที่ให้มา) เป็นไฟล์ต้นฉบับสำหรับสร้างไอคอนทุกขนาดที่ Android ต้องการ
ไม่ต้อง resize เองทีละไซส์ — ใช้เครื่องมือสร้างให้อัตโนมัติ

## ขั้นตอน (รันในเครื่องที่มี Node.js)

```bash
npm install @capacitor/assets --save-dev

# ต้องมี resources/icon.png (แนะนำ 1024x1024, พื้นหลังทึบ ไม่โปร่งใส)
# และ resources/splash.png (ไว้ทำหน้า splash screen ตอนเปิดแอพ — ใช้ไฟล์เดียวกันไปก่อนได้)

npx capacitor-assets generate --android
```

คำสั่งนี้จะสร้างไฟล์ไอคอนทุกขนาด (mdpi, hdpi, xhdpi, xxhdpi, xxxhdpi) ลงใน
`android/app/src/main/res/mipmap-*/` ให้อัตโนมัติ รวมถึง adaptive icon
(ไอคอนวงกลม/สี่เหลี่ยมมน ตามที่เครื่องแต่ละรุ่นตั้งไว้)

## หมายเหตุ

- โลโก้ที่ให้มาเป็นรูปวงกลมพื้นแดงมีดาวตรงกลาง — ระบบ adaptive icon ของ Android
  จะ crop ขอบเล็กน้อยเป็นวงกลม/มน ตามธีมเครื่อง ควรเช็คว่าดาวไม่ชิดขอบเกินไป
- ถ้าไฟล์ต้นฉบับมีพื้นหลังโปร่งใส Android จะเติมพื้นขาวให้เอง — โลโก้นี้พื้นแดงทึบอยู่แล้วไม่มีปัญหา
- รันคำสั่งนี้**หลัง** `npx cap add android` เท่านั้น (ต้องมีโฟลเดอร์ `android/` อยู่ก่อน)
- เพิ่มขั้นตอนนี้ใน GitHub Actions ได้ทีหลัง ถ้าอยากให้ auto-generate ทุกครั้งที่ build
